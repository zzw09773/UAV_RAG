# ========================================
# RAG System Environment Configuration
# ========================================

# ----- PostgreSQL Database Settings -----
# Connection URL for PGVector database
PGVECTOR_URL="postgresql+psycopg2://postgres:postgres@localhost:5433/postgres"

# Individual Postgres settings (used by docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
PG_PORT=5433

# ----- Embedding Model API Settings -----
# API key for embedding service (replace with your actual key)
EMBED_API_KEY=your_embedding_api_key_here

# Base URL for embedding API endpoint
EMBED_API_BASE=http://your-api-server/v1

# Embedding model name (nvidia/nv-embed-v2 recommended)
EMBED_MODEL_NAME=nvidia/nv-embed-v2

# ----- Chat/LLM API Settings -----
# API key for chat/generation service (can be same as EMBED_API_KEY)
LLM_API_KEY=your_llm_api_key_here

# Base URL for LLM API endpoint
LLM_API_BASE=http://your-api-server/v1

# Chat model name for answer generation
CHAT_MODEL_NAME=openai/gpt-oss-20b

# ----- Optional Settings -----
# Uncomment and configure if needed
# SSL_VERIFY=false
# LOG_LEVEL=INFO